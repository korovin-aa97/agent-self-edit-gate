## Summary

## Security invariant or user problem

## Validation

- [ ] Added/updated regression tests
- [ ] `ruff check` and `ruff format --check` pass
- [ ] strict `mypy` passes
- [ ] full `pytest` suite passes
- [ ] package builds and installs cleanly
- [ ] changelog/docs updated where needed
- [ ] no security claim depends on an unrestricted agent's own output
